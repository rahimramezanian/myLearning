  import json

To read from and write to json file


  f = open("fi","r")
  a = json.load(f)
  f.close()
  
  fw = open("fo","w")
  json.dump(a,fw,ensure_ascii=False)
  fw.close()


To check if a point is in polygon

from shapely.geometry import Point
from shapely.geometry.polygon import Polygon

point = Point(0.5, 0.5)
polygon = Polygon([(0, 0), (0, 1), (1, 1), (1, 0)])
print(polygon.contains(point))


To List Files
   import os
   os.listdir()



